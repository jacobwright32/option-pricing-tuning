[project]
name = "option-pricing-tuning"
version = "0.1.0"
description = "Autonomous options pricing research for CFD signal generation"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "scipy>=1.10",
    "pandas>=2.0",
    "matplotlib>=3.7",
]
